{
  "name": "ava-omniparser-prototype",
  "version": "0.0.1",
  "private": true,
  "description": "Session 3 / Prototype F1 — OmniParser v2 integration via Replicate. Measures real latency, output shape, and cost per call for the grounding tier.",
  "type": "module",
  "scripts": {
    "run": "tsx run.ts"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
