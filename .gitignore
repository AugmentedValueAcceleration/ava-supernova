node_modules/
dist/
*.tsbuildinfo
.env
.env.*
*.log
.DS_Store
Thumbs.db
coverage/
.vscode/
!.vscode/extensions.json
*.vsix
.claude/
.ava/

# Downloaded IDE plugins (regenerated by `yarn download:plugins`)
packages/plugins/

# Planning docs — private, not for public repo
/docs/

# scratch esbuild bundles
*.bb.mjs
.bb.mjs

# Decisions — Ava's project-context folder. Committed in USER projects (it is
# their shared direction); never in ours. Ours is working notes and plans in
# progress, and it is not the place a reader should learn what we are doing.
Decisions/
